# expense-tracking-system
A lightweight, command-line Expense Tracker built with Python and CSV file persistence. Tracks personal spending, validates inputs, and generates category-wise expenditure summaries without relying on external dependencies.
